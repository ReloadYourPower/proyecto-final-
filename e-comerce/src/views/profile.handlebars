 <div class="container">
            <div class="alert alert-success" role="alert">
                <p>Bienvenido, estás autenticado.</p>
            </div>
        </div>
   
<br>
<!-- views/profile.handlebars -->
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-header bg-primary text-white text-center">
                    <h2>Perfil de Usuario</h2>
                </div>
                <div class="card-body text-center">
                    <img src="{{user.avatar}}" alt="Avatar de {{user.name}}" class="rounded-circle mb-3" width="150">
                    <h3>{{user.name}}</h3>
                    <p><strong>Email:</strong> {{user.email}}</p>
                    <p><strong>Ubicación:</strong> {{user.location}}</p>
                    <p><strong>Biografía:</strong> {{user.bio}}</p>
                        <div class="container">
                            <button id="showAlertButton" class="btn btn-primary">current</button>
                        </div>
                        <br>
                        <div class="container">
                            <button id="showAlertButton2" class="btn btn-primary">users</button>
                        </div>
                        <br>
                        <div class="container">
                            <button id="showAlertButton3" class="btn btn-primary">Premium</button>
                        </div>
                    
                </div>
            </div>
        </div>
    </div>
    
   




  
       
  
  

